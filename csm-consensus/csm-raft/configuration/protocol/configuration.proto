syntax = "proto3";

package raft;

message RawServer
{
  string id = 1;
}

message RawSimpleConfiguration
{
  repeated RawServer servers = 1;
}

message RawClusterConfiguration
{
  RawSimpleConfiguration prevConfiguration = 1;
  RawSimpleConfiguration nextConfiguration = 2;
}